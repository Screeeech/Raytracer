{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "codelldb",
      "request": "launch",
      "name": "Debug C++",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build-debug",
      "postDebugTask": "clean",
      "expressions": "native",
      "program": "${workspaceFolder}/build/raytracer-starter"
    }
  ]
}
